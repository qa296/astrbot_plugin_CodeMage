{
  "static_code_audit": {
    "description": "启用针对AstrBot插件的静态代码审查（ruff + pylint + mypy）",
    "type": "bool",
    "hint": "开启后将对 LLM 生成的 main.py 执行 ruff/pylint/mypy 的静态检查并结合自定义规则进行报告（不阻断流程）",
    "default": true
  }
}
